# Ingress
This project creates an AWS EKS cluster on Fargate, configures IAM and the Load Balancer Controller, and deploys the 2048 game app. It uses eksctl, kubectl, and Helm to automate cluster setup, networking, and app deployment in a clean reproducible workflow.
